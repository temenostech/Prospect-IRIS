package com.temenos.marketplace;

import static org.junit.Assert.*;

import org.junit.Test;

/**
 * TODO: Document me!
 *
 * @author jfiricel
 *
 */
public class TestDifferentCommands {

    /*
    @Test
    public void test() {
        String textToBeCompared = "US AA US";
        String textToMatch1 = "(?!AU)";
        assertFalse(textToBeCompared.matches(textToMatch1));
        String textToMatch2 = "(?!US)";
        assertFalse(textToBeCompared.matches(textToMatch2));
        String textToMatch5 = "(.*)AA(.*)";
        assertTrue(textToBeCompared.matches(textToMatch5));
        String textToMatch4 = "(.*)AU(.*)";
        assertFalse(textToBeCompared.matches(textToMatch4));
        String textToMatch3 = "(.*)US(.*)";
        assertTrue(textToBeCompared.matches(textToMatch3));
        String textToMatch6 = "(U?S)";
        assertFalse(textToBeCompared.matches(textToMatch6));
        String textToMatch7 = "(.*)US(.*)";
        assertFalse(textToBeCompared.matches(textToMatch7));
        System.out.println(textToBeCompared.matches(textToMatch1));
        System.out.println(textToBeCompared.matches(textToMatch2));
        System.out.println(textToBeCompared.matches(textToMatch3));
        System.out.println(textToBeCompared.matches(textToMatch4));
        System.out.println(textToBeCompared.matches(textToMatch5));
        System.out.println(textToBeCompared.matches(textToMatch6));
        System.out.println(textToBeCompared.matches(textToMatch7));
    }
    */
}
